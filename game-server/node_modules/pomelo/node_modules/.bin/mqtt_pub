#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.0.3.9@mqtt/bin/mqtt_pub" "$@"
  ret=$?
else 
  node  "$basedir/../../../.0.3.9@mqtt/bin/mqtt_pub" "$@"
  ret=$?
fi
exit $ret
